sudo : required
language: generic
services:
    stdin_open: true
    - docker

before_install:
    - docker build -t jouncato/docker-react -f Dockerfile.dev . 

script:
    - docker run jouncato/docker-react npm run test -- --coverage
